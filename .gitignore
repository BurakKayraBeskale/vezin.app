node_modules/
.next/
*.node

# Veritabanı — asla commit'leme (canlı veri burada)
prisma/dev.db
prisma/dev.db-journal
prisma/dev.db-shm
prisma/dev.db-wal

# Yedekler — büyük binary dosyalar, repo dışında tut
prisma/backup/

# Ortam değişkenleri
.env
.env.local
.env.production.local

# NOT: prisma/migrations/ klasörü kasıtlı olarak burada YOK.
# Migration dosyaları Git'e eklenmeli — production deploy güvencesi.
